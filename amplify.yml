version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd Front-Angular/Front
        - npm ci
    build:
      commands:
        - npm run build -- --configuration production --project Front
  artifacts:
    baseDirectory: Front-Angular/Front/dist/browser
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
