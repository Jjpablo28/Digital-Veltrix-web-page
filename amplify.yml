frontend:
  phases:
    preBuild:
      commands:
        - cd Front-Angular/Front
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: Front-Angular/Front/dist/Front
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
