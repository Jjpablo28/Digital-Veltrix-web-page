version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd Front-Angular/Front
        - npm ci
    build:
      commands:
        - npm run build -- --configuration production
  artifacts:
    baseDirectory: dist/front
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
